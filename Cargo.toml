[workspace]
members = [
    "crates/memorose-common",
    "crates/memorose-core",
    "crates/memorose-server",
    "crates/memorose-gateway",
]
resolver = "2"

[profile.release]
# Helps reduce memory usage and recursion depth issues during build
codegen-units = 1
